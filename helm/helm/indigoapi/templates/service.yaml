apiVersion: v1
kind: Service
metadata:
  name: {{ include "indigoapi.fullname" . }}
  labels:
    {{- include "indigoapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "indigoapi.selectorLabels" . | nindent 4 }}
