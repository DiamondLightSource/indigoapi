apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ include "indigoapi.fullname" . }}-config"
  labels:
    app: "{{ include "indigoapi.name" . }}"

data:
  config.yaml: |-
{{ toYaml .Values.config | indent 4 }}
