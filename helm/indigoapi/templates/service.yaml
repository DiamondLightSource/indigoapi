apiVersion: v1
kind: Service
metadata:
  name: indigoapi
spec:
  type: { { .Values.service.type } }
  selector:
    app: indigoapi
  ports:
    - protocol: TCP
      port: { { .Values.service.port } }
      targetPort: { { .Values.service.port } }
